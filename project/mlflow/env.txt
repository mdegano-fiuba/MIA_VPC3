# Configuración interna del contenedor

# Host donde MLflow escuchará dentro del contenedor
MLFLOW_HOST=0.0.0.0

# Puerto interno del contenedor (y que se exportará)
MLFLOW_PORT=5000

# Ruta interna donde MLflow guardará artefactos (dentro del contenedor)
MLFLOW_ARTIFACTS=/app/mlruns


# Configuración externa (máquina host)

# Ruta local donde están los artefactos existentes
MLRUNS_DIR=./mlruns

# Log Level
MLFLOW_LOG_LEVEL=INFO

